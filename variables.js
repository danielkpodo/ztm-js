// Variables add as a storage entiy for items we might use in the future

//Variables have some special naming rules you need to follow

// Undefined happens when a varibale is not assign a varible

var basicSalary = "$200,000";

var fmt_salary = basicSalary.replace("$", "");
console.log(fmt_salary);
