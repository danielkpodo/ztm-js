//Scope means what variables do I have access to?
//Functions have access to variables within the root scope  or window scope

//when you run a function it always checks the parent scope or global scope if a variable named in its child scope exists there
